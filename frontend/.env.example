# Куда SPA ходит за API. Пустой префикс в проде, где Nginx проксирует /api сам.
VITE_API_URL=/api/v1

# Куда dev-сервер Vite проксирует /api. Порт 8001, а не 8000, потому что
# на машине разработчика 8000 бывает занят.
VITE_API_PROXY_TARGET=http://localhost:8001
